import { new_board, set_box } from "./board.ts";

// Make random changes to the board until we have
// exaustively search all posibilities for solutions
function brute_force_solve() {
	const board = new_board();
}
